<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:keep="@layout/exo_styled_player_view">

    <androidx.media3.ui.AspectRatioFrameLayout android:id="@id/exo_content_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <!-- Video surface will be inserted as the first child of the content frame. -->

        <View android:id="@id/exo_shutter"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black"/>

        <ImageView android:id="@id/exo_artwork"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"/>

        <ProgressBar android:id="@id/exo_buffering"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            android:layout_gravity="center"/>

        <TextView android:id="@id/exo_error_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="@dimen/exo_error_message_margin_bottom"
            android:gravity="center"
            android:textColor="@color/exo_white"
            android:textSize="@dimen/exo_error_message_text_size"
            android:lineSpacingMultiplier="1.1"
            android:background="@drawable/exo_rounded_rectangle"
            android:paddingLeft="@dimen/exo_error_message_text_padding_horizontal"
            android:paddingRight="@dimen/exo_error_message_text_padding_horizontal"
            android:paddingTop="@dimen/exo_error_message_text_padding_vertical"
            android:paddingBottom="@dimen/exo_error_message_text_padding_vertical"/>

    </androidx.media3.ui.AspectRatioFrameLayout>

    <androidx.media3.ui.SubtitleView android:id="@id/exo_subtitles"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <FrameLayout android:id="@id/exo_ad_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <FrameLayout android:id="@id/exo_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <com.brouken.player.dtpv.youtube.YouTubeOverlay
        android:id="@+id/youtube_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        app:yt_backgroundCircleColor="@color/ui_controls_background"
        app:yt_playerView="@id/video_view" />

    <View android:id="@id/exo_controller_placeholder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!--
         The spinner, with a word under it, after the controller and therefore
         drawn over it.

         A circle on its own says "something is happening" and leaves you to
         guess whether that is loading, seeking, or nothing at all, so it has a
         label; the two appear and disappear together.

         Outside the controller on purpose. It used to be a child of the centre
         controls row, which meant hiding the controls hid it as well — and the
         moment you most want to know the film is still loading is exactly the
         moment you have pressed Back to get the furniture out of the way.
         Nothing here is tied to the controls: only the playback state shows or
         hides it.
    -->
    <LinearLayout android:id="@+id/loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <ProgressBar
            style="?android:attr/progressBarStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:shadowColor="#FF000000"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="4"
            android:text="@string/buffering"
            android:textColor="@android:color/white"
            android:textSize="13sp"/>

    </LinearLayout>

    <!--
         The frame you are dragging towards.

         Above the bar rather than beside the thumb: on a phone the thumb is
         under a finger, and on a television there is no thumb to be beside.
         Centred, so it never runs off either end.

         Out here with the spinner and for the same reason — it is drawn over
         the controller instead of inside it, so nothing about the controls'
         own showing and hiding can take it away mid-drag.
    -->
    <ImageView android:id="@+id/thumbnail_preview"
        android:layout_width="180dp"
        android:layout_height="101dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="64dp"
        android:background="@android:color/black"
        android:contentDescription="@string/pref_scrub_thumbnails"
        android:scaleType="centerCrop"
        android:visibility="gone"/>

</merge>
