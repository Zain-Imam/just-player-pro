<?xml version="1.0" encoding="utf-8"?>
<!-- One cell of the poster grid.

     The whole cell is the focusable thing, not the image inside it, so a D-pad
     lands on something the size of a poster rather than something the size of an
     icon.

     The title is fixed at two lines so every cell is the same height and the
     rows of the grid line up, whatever the titles happen to be. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/online_poster_focus"
    android:focusable="true"
    android:clickable="true"
    android:padding="6dp">

    <ImageView
        android:id="@+id/poster"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:adjustViewBounds="true"
        android:background="@drawable/online_poster_placeholder"
        android:contentDescription="@null"
        android:scaleType="centerCrop" />

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:minLines="2"
        android:textColor="@color/white"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#B3FFFFFF"
        android:textSize="11sp" />

</LinearLayout>
