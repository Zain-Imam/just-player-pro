*.iml
.gradle
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Local secrets for development/testing — never commit.
.env
.env.*
!.env.example

# Kotlin build-tool scratch, not source.
.kotlin/

# Built APKs staged for a release.
/release-apks/

# Release signing. The keystore and its passwords never go in the repository;
# losing them means no user can ever install an update over this build.
/keystore/
/keystore.properties

# Video the smoke test downloads to drive the player with.
/.smoke-media/

# Local planning note, not part of the project
v4.1.0Roadmap.md
